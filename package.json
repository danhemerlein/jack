{
  "name": "jack",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://danhemerlein.github.io/jack/",
  "dependencies": {
    "classnames": "^2.2.6",
    "contentful": "^7.5.1",
    "gh-pages": "^2.0.1",
    "lodash": "^4.17.11",
    "node-sass": "^4.12.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.0"
  },
  "scripts": {
    "start": "NODE_PATH=src react-scripts start",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build",
    "build": "NODE_PATH=src react-scripts build",
    "test": "NODE_PATH=src react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "basement": "git+https://github.com/sanctuarycomputer/basement.git"
  }
}
